"use client"
import{useEffect,useState}from"react";import{createGoogleCustomer,finishGoogleLogin,retrieveCustomer}from"../../lib/medusa";import{useCommerce}from"../../components/CommerceProvider"
function decode(token:string){try{return JSON.parse(atob(token.split(".")[1].replace(/-/g,"+").replace(/_/g,"/")))}catch{return{}}}
export default function GoogleCallback(){const{setAuthToken}=useCommerce();const[message,setMessage]=useState("Свързваме Google профила…");useEffect(()=>{(async()=>{try{let token=await finishGoogleLogin(window.location.search.slice(1));try{await retrieveCustomer(token)}catch{const payload=decode(token);const email=payload?.user_metadata?.email;if(!email)throw new Error("Google не върна имейл адрес.");token=await createGoogleCustomer(token,email)}await setAuthToken(token);window.location.replace("/account")}catch(e){setMessage(e instanceof Error?e.message:"Google входът не беше успешен.")}})()},[setAuthToken]);return <main className="auth-page"><section className="auth-card"><h1>Google вход</h1><p>{message}</p><a href="/account">Обратно към вход</a></section></main>}
