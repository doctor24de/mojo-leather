"use client"
import{useState,type FormEvent}from"react";import{resetPassword}from"../../lib/medusa"
export default function ResetPassword(){const[message,setMessage]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),q=new URLSearchParams(window.location.search);try{await resetPassword(q.get("token")||"",q.get("email")||String(f.get("email")),String(f.get("password")));setMessage("Паролата е сменена. Вече можеш да влезеш.")}catch(error){setMessage(error instanceof Error?error.message:"Линкът е невалиден.")}};return <main className="auth-page"><section className="auth-card"><h1>Нова парола</h1><form onSubmit={submit}><label>Имейл<input name="email" type="email" required/></label><label>Нова парола<input name="password" type="password" minLength={8} required/></label><button className="button dark auth-submit">Запази паролата</button></form>{message&&<p role="status">{message}</p>}</section></main>}
