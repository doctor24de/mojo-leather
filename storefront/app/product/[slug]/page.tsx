import{notFound}from"next/navigation";import{formatPrice,products}from"../../data"
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)notFound();return <main className="product-page"><div className="product-gallery"><img src={product.image} alt={`Кожено яке ${product.name}`}/></div><div className="product-detail"><p className="eyebrow">{product.category} · {product.color}</p><h1>{product.name}</h1><strong className="product-price">{formatPrice(product.price)}</strong><p className="product-description">{product.description}</p><div className="sizes"><span>Избери размер</span><div>{["XS","S","M","L","XL"].map(s=><button key={s}>{s}</button>)}</div></div><button className="add-to-cart">Добави в количката</button><details open><summary>Материал и изработка</summary><p>100% естествена кожа. Вискозна подплата. Произведено в малка серия.</p></details><details><summary>Доставка и връщане</summary><p>Безплатна доставка над €250 и 30 дни за връщане.</p></details></div></main>}
